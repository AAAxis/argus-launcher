// SCRATCH — see vite.preview.config.ts. Nothing on the previewed surfaces
// notifies, so these only need to exist.
export function composeFinishMessage() {
  return '';
}
export function composeFinishTelegram() {
  return '';
}
export function shouldNotify() {
  return false;
}
